import React from 'react';
import useCart from '../../hooks/useCart';

const Booking = () => {
    const cart = useCart();
    
    return (
        <div>
            hello booking
        </div>
    );
};

export default Booking;